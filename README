Global setup:

 Set up git
  git config --global user.name "Your Name"
  git config --global user.email blaze3j@yahoo.com
      

Next steps:

  mkdir DocHunt
  cd DocHunt
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:blaze3j/DocHunt.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:blaze3j/DocHunt.git
  git push -u origin master
      


